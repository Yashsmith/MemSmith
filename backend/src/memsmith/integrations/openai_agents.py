"""OpenAI Agents SDK integration seam."""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(slots=True)
class MemSmithStore:
    """Placeholder integration object for OpenAI Agents SDK."""

    session_id: str
